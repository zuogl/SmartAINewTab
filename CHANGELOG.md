# 更新记录

本项目采用语义化版本号。公开发布前的 `0.x` 版本仍可能调整数据结构或界面，但会尽量
保持本地备份和云备份向后兼容。

## Unreleased

### Added

- 扩展语言设置：支持跟随浏览器、简体中文、繁體中文、日本語、한국어 和 English，并可在设置中即时切换。
- Chrome Manifest 名称与说明的 `_locales` 本地化资源。
- 公开隐私政策、使用条款、支持和账户删除页面。
- 扩展内云备份删除与账户删除入口。
- 云备份 R2 存储、D1 元数据和持久化删除任务。
- Provider 响应大小/超时限制以及统一公网 URL 安全策略。
- OAuth client state、PKCE、短期滥用限流和最近登录删除保护。
- 开源贡献、安全政策、CI 与 Chrome Web Store 提交清单。

### Changed

- 设置页、搜索与命令面板、背景、组件和主要书签管理界面支持五种语言；用户书签和 AI 生成内容保持原文。
- HTTP(S) 网站访问改为运行时可选权限。
- favicon、页面元数据和普通书签体检明确不携带 Cookie。
- 云会话默认期限缩短为 7 天。
- 清理临时 QA 截图、私人导出、生成缓存和嵌套仓库历史。

### Security

- 阻止导入备份静默替换 Provider Key 或云服务地址。
- 阻止远程 HTTP Provider、内网/保留地址和不安全跨站跳转。
- 在解析前限制 Worker 请求体，并为 AI 响应设置 1 MiB 上限。
- 防止备份写入与账户/R2 删除之间的竞态。
